<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@drawable/bg_drawable</item>
        <item name="android:statusBarColor">@color/status_bar_theme</item>
    </style>

    <style name="SplashScreen_SplashAnimation">
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

    <style name="SplashScreen_SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowAnimationStyle">@style/SplashScreen_SplashAnimation</item>
        <item name="android:windowBackground">@drawable/bg_drawable</item>
        <item name="android:statusBarColor">@color/status_bar_theme</item>
    </style>


    <style name="zopim_toolbar">
        <item name="android:background">@android:color/black</item>
        <!--<item name="android:minHeight">@color/actionBarSize</item>-->
        <!--<item name="popupTheme">@style/ThemeOverlay.AppCompat.Light</item>-->
    </style>

    <!-- Base chat sdk theme. -->
    <style name="ZopimChatTheme" parent="ZopimChatTheme.Light">
        <!-- Customize chat theme here. -->
        <item name="colorAccent">@color/primary</item>
        <item name="colorPrimary">@color/primary</item>
        <!--<item name="colorPrimaryDark">@color/primary_dark</item>-->
    </style>

    <style name="no_agents_button">
        <item name="android:background">@color/primary</item>
        <item name="android:padding">8dp</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!--Customize toolbar that replaces action bar-->
    <!--<style name="zopim_toolbar">-->
        <!--<item name="android:background">?attr/primary</item>-->
        <!--<item name="android:minHeight">?attr/actionBarSize</item>-->
        <!--<item name="android:theme">@style/ThemeOverlay.AppCompat.Dark.ActionBar</item>-->
        <!--<item name="popupTheme">@style/ThemeOverlay.AppCompat.Light</item>-->
    <!--</style>-->

    <!--Customize where chat widget appears-->
    <style name="chat_widget" parent="_chat_widget">
        <item name="anchor">bottom_left</item>
    </style>

    <!--Customize chat colors-->
    <color name="chat_bubble_visitor">@color/primary</color>
</resources>
